To all sqlwindow developers:

This zip files contains:
sqlwin.app     SQLwindow sample application
pestruca.dll   intermidate dll used to make structured pe calls
oridinal.doc   ms word document about the CRPE.dll's ordinal numbers

You need to place pestruca.dll into the windows\system directory and 
make all neccessary changes like: report file name, servername, userid
and password.

